# 核心验收参考样本：公开审查摘要

本 PR 仅供审查交付摘要与是否合并，不包含完整验收规格或样本。
完整参数、固定输入/预期、核算器、映射及项目记忆继续仅保留本地。

## 本次交付

本地 CORE_ORACLES_V1 绑定已选 PARAMETERS_V1，固定 57 项合成参考向量。
输入、预期和容差单独保存；参考核算器不生成或改写预期。
冻结来源哈希检查通过，原规格、注册表、阶段图及原案例保持不变。
本地 57 项参考检查通过；应用验收执行数为 0，供应商实测数为 0。
本 PR 未包含脚本及完整样本，不能独立复跑上述检查。

| 范围 | 本批参考内容 | 仍待验证 |
|---|---|---|
| 金额与账本 | 舍入、正负分摊、行权/指派、拆股、资本返还、转入桥接 | 完整 FIFO 事件链、复杂公司行动与事务 |
| 收益率 | TWR 外部现金流、零/负基数、缺日分段；整数年 XIRR 多根 | 日期聚合、跨闰年、生产求解器与失败路径 |
| 四种策略 | CSP、CC、PCS、CCS 到期收益及滑点边界 | 提前指派与完整历史成交路径 |
| 定价与 IV | 欧式 BSM 参考价格及二分反演、部分拒绝条件 | 美式 CRR、离散股息、Greeks、生产 Brent 与完整套利边界 |
| B 路线 | 样本门槛、零回合、Wilson、分箱、Delta、非重叠与缺证排除 | 完整匹配契约、真实样本和数据许可 |
| 尾部 | 经验分布分数质量、带符号尾部均值 | 连续风险中性积分及收敛/预算限制 |

## 覆盖与批准边界

84 个原案例均保持 NOT_EXECUTED，不代表已有 84 个完整可执行测试。
AX 关联仅基于共享 CALC，不是完整情境或断言覆盖；AV/AP 尚未细化映射。
失败状态字符串仅表达语义，不是已确定的应用 enum。
参考核算通过不等于生产实现、金融模型、CI 或完整数值验收通过。
合并本 PR 不新增参数批准，也不关闭上述算法、覆盖或运行时缺口。

## 持续约束

DATA-000 保持 B：有限历史统计；Bootstrap 与校准保持禁用。
B 只读展示的选择不自动通过 B 出口；G5 仍未通过，供应商未获资格。
数据尽量免费，缺失如实不可用；不放宽时效、NBBO、PIT、feed 或许可要求。
不拼接不同 feed 冒充十年样本，账本 EOD 仍与回测成交数据分开。
后期 GPT API 仅为可选检索/提取/解释方向，不用生成值填补真实行情缺口。
不自动采购、调用付费 API、提前启动 AI 阶段或发送私人资料。
行情证据、提醒 48/64、恢复引用完整性/原子回滚和硬件性能仍待专门验证。

本 PR 不创建应用工程、不启动 Phase 0，也不改变已确认交付顺序。
下一步工程准备仍需确定最低 macOS、目标硬件与工程约束。
完整私人资料和私人 Git 历史不在本次公开授权内；等待用户审查后决定合并。
